import 'package:flutter/widgets.dart';

class EmbeddedMap extends StatelessWidget {
  const EmbeddedMap({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) => const Text('Harita yalnızca web’de.');
}
