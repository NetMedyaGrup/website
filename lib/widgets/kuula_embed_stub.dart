import 'package:flutter/widgets.dart';

class KuulaEmbedStub extends StatelessWidget {
  const KuulaEmbedStub({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) => const Text('360° tur yalnızca web’de.');
}
